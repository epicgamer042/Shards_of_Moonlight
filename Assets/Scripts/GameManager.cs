using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    [SerializeField] private List<GameObject> gameObjects; // Assign Level Prefabs in the 

    // Manage Level displayed

    // Manage Player spawn in level (get spawn location object from level prefab)
}
